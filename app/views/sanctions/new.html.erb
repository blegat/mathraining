<% provide(:title, 'Créer une sanction') %>

<h1><%= title_sanction(@user, "Créer une sanction") %></h1>

<%= form_for(@sanction, url: user_sanctions_path) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Créer", class: "btn btn-lg btn-primary" %>
<% end %>
