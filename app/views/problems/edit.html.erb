<% provide(:title, 'Modifier un problème') %>

<h1><%= title_problem(@problem, "Modifier") %></h1>

<%= form_for(@problem) do |f| %>
  <%= render 'form', f: f, problem: @problem %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
