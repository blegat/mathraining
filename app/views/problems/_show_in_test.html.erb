<!-- Enoncé -->
<h3>Énoncé</h3>

<div>
  <%= htmlise(@problem.statement) %>
</div>

<!-- Solution de l'étudiant -->
<h3 class="mt-4">Votre solution</h3>

<!-- Si une soumission a déjà été faite -->
<% if !@submission.new_record? %>
  <div id="the" class="myoverflow">
  <%= render 'shared/post', ms: @submission, kind: "submission_in_test", can_edit: true, reduce_margin_top: true %>
  </div>

<!-- Si pas encore de soumission -->
<% else %>
  <div id="the" class="myoverflow">
  <!-- On propose d'écrire une solution -->
  <p class="text-color-orange">Vous n'avez pas encore envoyé de solution à ce problème.</p>
  <p>N'hésitez pas à écrire tout ce que vous trouvez même s'il ne s'agit pas d'une solution complète au problème ! Lors d'un vrai test ou d'une vraie compétition, vous rendrez également vos feuilles de brouillon et celles-ci pourront vous rapporter des points si elles contiennent des éléments pouvant mener à une solution.</p>
  <center><button class="btn btn-ld-light-dark" onclick='return Rolling.develop("")'>Écrire une solution</button></center>
  </div>
<% end %>

<div id="form" class="myoverflow px-1" style="height:0px;">
<% if @submission.new_record? %>
  <%= render 'submissions/new_in_test' %>
<% else %>
  <%= render 'submissions/edit_in_test' %>
<% end %>
</div>

<% if @submission.errors.any? %>
  <script>rollingDevelopQuickWhenPossible("")</script>
<% end %>
