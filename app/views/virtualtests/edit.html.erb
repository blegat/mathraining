<% provide(:title, 'Modifier un test virtuel') %>

<h1>
<span class="title-first">Tests virtuels ></span>
Modifier un test virtuel
</h1>

<%= form_for @virtualtest do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
