<% provide(:title, 'Modifier un exercice') %>

<h1><%= title_4("Théorie", (link_to @question.chapter.section.name, @question.chapter.section), (link_to @question.chapter.name, chapter_question_path(@question.chapter, @question)), "Modifier un exercice") %></h1>

<%= form_for(@question) do |f| %>
  <%= render 'form', f: f, question: @question %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
