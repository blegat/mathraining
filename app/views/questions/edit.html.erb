<% provide(:title, 'Modifier un exercice') %>

<h1><%= title_question(@question, "Modifier un exercice") %></h1>

<%= form_for(@question) do |f| %>
  <%= render 'form', f: f, question: @question %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
