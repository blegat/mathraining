Merci d'écrire votre solution directement sur le site, en utilisant le $\LaTeX$ pour les formules mathématiques. <%= link_to "Ce chapitre", chapter_path(13), target: "blank" %> vous donne toutes les bases pour l'utiliser.<br/> Les solutions écrites à la main et scannées ne sont pas acceptées, les pièces jointes n'étant à utiliser que pour les figures ou schémas.<br/><br/>
  <div class="form-group">
  <div class="well" id="MathPreview"></div>
  <div class="well hidden-preview" id="MathBuffer"></div>
  <%= render 'subjects/smiley', hiddentext: false %>
  <% if @ancientexte.nil? %>
    <%= f.text_area :content, :class => "form-control", :maxlength => "8000", :style => "height:200px; margin-top:5px;", :id=>"MathInput", :onkeyup => "PreviewSafe.MyUpdate()", :disabled => (!@signed_in || current_user.other) %>
  <% else %>
    <%= f.text_area :content, :class => "form-control", :maxlength => "8000", :style => "height:200px; margin-top:5px;", :value => @ancientexte, :id=>"MathInput", :onkeyup => "PreviewSafe.MyUpdate()", :disabled => (!@signed_in || current_user.other) %>
  <% end %>
  </div>
