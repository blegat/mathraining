<% provide(:title, 'Modifier un point théorique') %>

<h1><%= title_theory(@theory, "Modifier un point théorique") %></h1>

<%= form_for(@theory) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
