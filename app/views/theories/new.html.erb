<% provide(:title, 'Créer un point théorique') %>

<h1><%= title_4("Théorie", (link_to @chapter.section.name, @chapter.section), (link_to @chapter.name, @chapter), "Créer un point théorique") %></h1>

<%= form_for(@theory, url: chapter_theories_path) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Créer", class: "btn btn-lg btn-primary" %>
<% end %>

<script>
Preview.Init();
Preview.Update();
</script>
