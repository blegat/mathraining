<% provide(:title, 'Ajouter une question') %>

<h1><%= title_faqs("Ajouter une question") %></h1>

<%= form_for(@faq) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Créer", class: "btn btn-primary btn-lg" %>
<% end %>
