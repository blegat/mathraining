<% provide(:title, 'Modifier une question') %>

<h1><%= title_faqs("Modifier une question") %></h1>

<%= form_for(@faq) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
