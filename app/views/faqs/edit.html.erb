<% provide(:title, 'Modifier une question') %>

<h1><%= title_2((link_to "Questions fréquemment posées", faqs_path), "Modifier une question") %></h1>

<%= form_for(@faq) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
