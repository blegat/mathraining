<% mois = ["janvier", "février", "mars", "avril", "mai", "juin", "juillet", "août", "septembre", "octobre", "novembre", "décembre"] %>

  <table class="table table-bordered" style="margin-top:20px;">
  <tr class="info">
  <td style="font-size:26px; font-weight:bold; padding:15px; border-right:none;"><%= f.title %> </td>

  <td style="text-align:right; font-size:18px; padding:15px; border-left:none;">
  <% date = f.created_at.in_time_zone %>
  <%= date.day %> <%= mois[date.month-1] %> <%= date.year %> à <%= date.hour %>h<%= "0" if date.min < 10 %><%= date.min %>
  </td>

  </tr>
  <tr><td style="padding:15px;" colspan="2">
  <div>
  <%= raw(f.content.gsub(/<hr>[ \r]*\n/,'<hr>').gsub(/\\][ \r]*\n/,'\] ').gsub(/\$\$[ \r]*\n/,'$$ ').gsub(/<\/h2>[ \r]*\n/,'</h2>').gsub(/<\/h3>[ \r]*\n/,'</h3>').gsub(/<\/h4>[ \r]*\n/,'</h4>').gsub(/<\/ol>[ \r]*\n/, '</ol>').gsub(/\n[ \r]*<\/ol>/, '</ol>').gsub(/<\/ul>[ \r]*\n/, '</ul>').gsub(/\n[ \r]*<\/ul>/, '</ul>').gsub(/\n[ \r]*<li>/, '<li>').gsub(/\n/, '<br/>')) %>
  </div>
  </td></tr>

  <% if current_user.sk.admin? %>
    <tr><td colspan="2">
    <center>
      <%= link_to "Modifier cette actualité", edit_actuality_path(f) %> |
      <%= link_to "Supprimer cette actualité", f, method: :delete, data: { confirm: "Etes-vous sûr de vouloir supprimer cette actualité?" } %>
    </center>
    </td></tr>
  <% end %>
  </table>

