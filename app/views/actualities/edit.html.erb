<% provide(:title, 'Modifier une actualité') %>

<h1><%= title_2((link_to "Actualités", root_path), "Modifier une actualité") %></h1>

<%= form_for(@actuality) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
