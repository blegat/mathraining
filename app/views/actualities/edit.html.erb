<% provide(:title, 'Modifier une actualité') %>

<h1><%= title_actualities("Modifier une actualité") %></h1>

<%= form_for(@actuality) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
