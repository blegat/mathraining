<% provide(:title, "Modifier une section") %>

<h1>
<span style="font-size:16px;">Cours ></span>
<span style="font-size:24px;"><%= link_to @section.name, @section %> ></span>
Modifier
</h1>

    <%= form_for(@section) do |f| %>
      <%= render 'form', f: f %>

      <%= f.submit "Editer", class: "btn btn-lg btn-primary" %>
    <% end %>

<script>
Preview.Init();
Preview.Update();
</script>
