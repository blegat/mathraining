<% provide(:title, "Notifications") %>
<h1>
  <% if @new %>
    Toutes les soumissions
  <% else %>
    Soumissions auxquelles vous participez
  <% end %>
</h1>

Veuillez commencer par les soumissions les plus anciennes, c'est-à-dire le plus bas dans la liste suivante.<br/><br/>

<center>
  <%= will_paginate @notifications, :style => "margin-top:-10px;", :class => "hidden-xs" %>
  <%= will_paginate @notifications, :style => "margin-top:-10px;", :class => "visible-xs", :previous_label => "Préc.", :next_label => "Suiv." %>
</center>

<!-- Si on affiche toutes les soumissions -->
<% if @new %>

  <% @situation = 1 %>
  <div class="row">
  <div class="col-sm-12 col-xs-12">
  <table class="table table-bordered">
  <tr><th class="hidden-xs" style="text-align:center;">Statut</th><th class="hidden-xs">Date de la soumission</th><th>Dernière activité</th><th>Qui</th><th class="hidden-xs">Correcteur(s)</th><th></th></tr>
  <% @notifications.each do |n| %>
    <%= render 'submissions/showadmin', s: n, nom: true %>
  <% end %>
  </table>
  </div>
  </div>

<!-- Si on affiche les soumissions auxquelles on participe -->
<% else %>

  <% @situation = 2 %>
  <div class="row">
  <div class="col-xs-12 col-sm-12">
  <table class="table table-bordered">
  <tr><th class="hidden-xs" style="text-align:center;">Statut</th><th class="hidden-xs">Date de la soumission</th><th>Dernière activité</th><th>Qui</th><th></th></tr>
  <% @notifications.each do |n| %>
    <%= render 'submissions/showadmin', s: n, nom: true %>
  <% end %>
  </table>
  </div>
  </div>
<% end %>

<center>
  <%= will_paginate @notifications, :style => "margin-top:-10px;", :class => "hidden-xs" %>
  <%= will_paginate @notifications, :style => "margin-top:-10px;", :class => "visible-xs", :previous_label => "Préc.", :next_label => "Suiv." %>
</center>

