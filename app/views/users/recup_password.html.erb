<% provide(:title, "Nouveau mot de passe") %>

<h1><%= title_1("Nouveau mot de passe") %></h1>

<p>Veuillez rentrer un nouveau mot de passe. (Utilisateur : <b><%= @user.fullname %></b>)</p>

<%= form_for(@user, url: change_password_user_path(@user, :key => @user.key)) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= render "password_inputs", f: f %>
  
  <%= f.submit "Modifier le mot de passe", class: "btn btn-lg btn-primary mb-3" %>
<% end %>
