<script type="text/javascript">
function checkChecked() {
    var c1 = document.getElementById("consent1").checked;
    var c2 = document.getElementById("consent2").checked;
    var bt = document.getElementById("send-button");
    var war = document.getElementById("warning");
    if(c1 && c2) {
      warning.style.color = "white";
      bt.disabled = false;
    }
    else {
      warning.style.color = "red";
      bt.disabled = true;
    }
}
</script>

<% provide(:title, 'Inscription') %>

<div style="padding:15px; background-color:#eeeeee; border:1px solid #dddddd; margin-top:15px;">
<h3 style="margin-top:0px;">À lire avant l'inscription</h3>
Mathraining est un site interactif sur lequel il est possible d'écrire une solution à un problème pour la faire corriger par un correcteur bénévole. Par respect pour les correcteurs, il est dès lors demandé à chacun de ne proposer que des solutions dont il est l'auteur, et qui n'ont donc jamais été soumise sur Mathraining auparavant. Les doubles comptes ne sont donc pas tolérés et seront systématiquement supprimés.<br/><br/>

Les administrateurs se réservent le droit de bloquer l'accès au site aux personnes mal intentionnées ou utilisant le site de manière inappropriée.
</div>

<h1><%= title_1("Inscription") %></h1>

Pour vous inscrire, il vous suffit de posséder une adresse e-mail valide. Un e-mail de confirmation vous sera envoyé à cette adresse.<br/><br/>

<%= form_for(@user) do |f| %>
  <%= render 'form', f: f, modif: false %>

  <%= f.submit "Créer mon compte", class: "btn btn-lg btn-primary", id: "send-button", disabled: !Rails.env.test? %>
  <div id="warning" style="margin-top:15px; margin-bottom:5px; color:red;">Pour pouvoir vous inscrire, vous devez accepter la politique de confidentialité.</div>
  <script>checkChecked()</script>
<% end %>
