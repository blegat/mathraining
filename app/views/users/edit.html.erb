<% provide(:title, "Actualisez votre profil") %> 
<h1>Actualisez votre profil</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <br />
      <%= f.submit "Mettre à jour", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
