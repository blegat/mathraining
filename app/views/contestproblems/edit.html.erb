<% provide(:title, 'Modifier un problème') %>

<h1><%= title_contestproblem(@contestproblem, "Modifier") %></h1>

<%= form_for(@contestproblem) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
