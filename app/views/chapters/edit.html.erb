<% provide(:title, 'Modifier un chapitre') %>

<h1><%= title_4("Théorie", (link_to @chapter.section.name, @chapter.section), (link_to @chapter.name, @chapter), "Modifier") %></h1>

<%= form_for(@chapter) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>

<script>
Preview.Init();
Preview.Update();
</script>
