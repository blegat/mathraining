<% provide(:title, 'Modifier une énigme') %>

<h1><%= title_puzzles("Modifier une énigme") %></h1>

<%= form_for(@puzzle) do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Modifier", class: "btn btn-lg btn-primary" %>
<% end %>
