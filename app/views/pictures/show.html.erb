<% provide(:title, 'Récupérer un url') %>

<h1><%= title_2((link_to "Vos images", pictures_path), "Récupérer un url") %></h1>

<center>
<%= image_tag @picture.image, :class => "mt-3", :style => "max-width:800px;" %>
</center>

<p class="mt-3 text-center"><%= picture_image_url(@picture, :only_path => true, :key => @picture.access_key) %></p>
