<% provide(:title, 'Uploader une image') %>

<h1><%= title_pictures("Uploader une nouvelle image") %></h1>

<p>
Votre image doit respecter les conditions suivantes :
</p>

<ul>
<li>Faire moins de 1 Mo</li>
<li>Être de type : gif, png, bmp, ou jp(e)g</li>
<li>Être jolie :-)</li>
</ul>


<%= form_for @picture, :url => pictures_path, :html => { :multipart => true } do |f| %>	
  <%= render 'shared/error_messages', object: @picture %>
  
  <!-- To avoid error when no picture is chosen -->
  <%= f.hidden_field :useless, value: 0 %>
  
  <div class="mb-2">
  <%= f.label "Choisissez votre image :", class: "form-label" %>
  <%= f.file_field :image, class: "form-control" %>
  </div>
  
  <%= f.submit "Uploader", class: "btn btn-primary" %>
<% end %>
